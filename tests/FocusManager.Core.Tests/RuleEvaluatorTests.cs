using Xunit;

namespace FocusManager.Core.Tests;

public sealed class RuleEvaluatorTests
{
    [Fact(Skip = "Stub test. Implement when RuleEvaluator logic is ready.")]
    public void EvaluateApplicationStart_ReturnsDecision_ForKnownPath()
    {
        Assert.True(true);
    }
}
