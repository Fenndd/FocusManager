<Page
    x:Class="FocusManager.App.Views.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Loaded="Page_Loaded">

    <ScrollViewer>
        <StackPanel Spacing="12" Padding="24">
            <TextBlock Text="FocusManager" FontSize="28" FontWeight="SemiBold" />
            <TextBlock Text="Control study mode and manage whitelist categories." />

            <StackPanel Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
                <ToggleSwitch
                    x:Name="StudyModeSwitch"
                    Header="Study Mode"
                    OnContent="ON"
                    OffContent="OFF"
                    Toggled="StudyModeSwitch_Toggled" />

                <Button x:Name="RefreshButton" Content="Refresh" Click="RefreshButton_Click" />
            </StackPanel>

            <TextBlock x:Name="StatusText" Text="Agent status: unknown" />
            <TextBlock x:Name="ModeStatusText" Text="Current mode: unknown" Opacity="0.8" />
            <TextBlock x:Name="ReadinessText" Text="Whitelist status: unknown" TextWrapping="Wrap" Opacity="0.85" />

            <StackPanel Orientation="Horizontal" Spacing="8">
                <Button x:Name="AppsButton" Content="Applications (?)" Click="OpenApps_Click" />
                <Button x:Name="FoldersButton" Content="Folders (?)" Click="OpenFolders_Click" />
                <Button x:Name="SitesButton" Content="Sites (?)" Click="OpenSites_Click" />
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Page>
